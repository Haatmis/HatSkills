---
type: tool_used
tool: Skill
input_match: '"skill"\s*:\s*"(?:[\w-]+:)?code"'
min: 0
max: 0
---

La frontière : `code` ne doit pas prendre ce terrain. S'il se déclenche, l'exclusion croisée entre `code` et `game-design` est mal posée — le remède est dans les descriptions, pas dans les corps.
