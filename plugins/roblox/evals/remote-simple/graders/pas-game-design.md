---
type: tool_used
tool: Skill
input_match: '"skill"\s*:\s*"(?:[\w-]+:)?game-design"'
min: 0
max: 0
---

La frontière : `game-design` ne doit pas prendre ce terrain. S'il se déclenche, l'exclusion croisée entre `game-design` et `code` est mal posée — le remède est dans les descriptions, pas dans les corps.
